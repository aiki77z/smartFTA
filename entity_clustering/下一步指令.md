接下来我需要让开发人员完成GNR和前端AI助手的多智能体部分落地。为了让他们更好地完成这件事（不受我们现在正在改动的KB部分的影响），我需要你完成数据的准备（这样他们做GNR模块的开发与调试的时候，看到的是“KB模块已完成”，neo4j和mongodb中都有相应数据。）

首先我需要规范你的neo4j存储格式。我们仍然让mention和entitycluster在同一张graph中，但是我们在GNR模块中具体使用时主要看的还是entitycluster而非mention（当具体溯源时才观察mention）
我需要最终的neo4j的entitycluster与Mention的节点类型和关系类型对应：
实体类型5 类：
1. 故障事件：设备异常、故障现象、故障原因、停机、失效、异常状态等。例如：编码器通信故障、反馈信号丢失、主轴保护停机。
2. 故障类别：对故障事件的类别归纳。例如：通信故障、温度故障、润滑系统故障。
3. 报警码：故障码、报警号、错误码。例如：F01000。
4. 维修方法：检查、处理、修复、观察、更换、紧固、清洗等措施。例如：更换编码器、重新紧固接头。
5. 触发规则：阈值、条件组合、持续时间、逻辑判断等规则。例如：温度超过80℃持续3秒。
   实体类型仍然保留Chunk和File，只是你需要把EntityCluster和Mention变为以上7类（仍然需要区分Entity和Mention）
关系类型7 类，并严格遵守起点和终点类型：
1. 故障触发：故障事件 -> 故障事件。例如：接头松动 -> 反馈信号丢失。
2. 故障表征：报警码 -> 故障事件。例如：F01000 -> 编码器通信故障。
3. 故障分类：故障事件 -> 故障类别。例如：编码器故障 -> 通信故障。
4. 故障处理：维修方法 -> 故障事件。例如：更换编码器 -> 编码器故障。
5. 规则触发：触发规则 -> 故障事件。例如：温度超过80℃持续3秒 -> 过温停机。
6. 参与组合：故障事件 -> 逻辑组。不要在 [RELATION] 中直接输出；只有遇到 AND 组合时才输出 [LOGIC_GROUP]。
7. 组合导致：逻辑组 -> 故障事件。不要在 [RELATION] 中直接输出；只有遇到 AND 组合时才输出 [LOGIC_GROUP]。
   关系类型仍然保留EVIDENCED_IN和RESOLVED_TO，只是你需要把CLUSTERED_RELATION和MENTION_RELATION拆成以上7类（仍然需要区分CLUSTERED_RELATION和MENTION_RELATION）。

请你写一个neo4j的新的存储格式方案文档。

执行cluster_entities.py后得到的中间产物需要包含聚类后的实体关系全部信息，当然还必须包括原始mention的全部信息，不允许丢失。这个中间产物会被我发给开发人员。
然后针对中间产物的数据导入脚本：能够把它们导入到neo4j（遵循上文规范的neo4j存储格式）及mongodb（可能需要导入top_catalog_event）。这个数据导入脚本将被作为最终正式KB模块的一部分。这个数据导入脚本放在FTA-KB下。

针对annotation-data-pipeline/zyt/HUAWEI LUNA2000-(97KWH-200KWH)系列 工商业构网型储能系统 告警参考_organized.csv中的数据，我需要你执行cluster_entities.py得到聚类后的中间产物。

然后你需要写一个从原始csv中解析出需要放进Mongodb的信息（chunks, file_versions, files, 等）并导入Mongodb的脚本。这个导入MongoDB的脚本不会被作为最终正式KB模块的一部分，它只是为现在的开发形式服务的。

开发人员需要：
1.用导入MongoDB的脚本，将原始csv中的原文信息导入MongoDB。
2.用我发给他们的中间产物和数据导入脚本，将中间产物导入neo4j与mongodb。
关键是需要模拟实际流程中KB模块完成后的结果。

你可以看看KB模块中原本将数据导入mongodb和neo4j的那部分是怎么写的，但是因为现在neo4j中存储格式发生了变化，你需要按新的neo4j存储方案将中间产物导入进去，我还会把你写的neo4j的新的存储格式方案文档交给开发人员让他们阅读来具体修改GNR的实现方式。
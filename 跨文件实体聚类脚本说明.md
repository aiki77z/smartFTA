# 跨文件实体聚类脚本说明

本文档说明 `cross_file_entity_clustering.py` 的作用和运行方式。

## 1. 脚本定位

`cross_file_entity_clustering.py` 是 KB 模块中的一个步骤，用于在“新上传文件已经完成文件内聚类”之后，把新文件的聚类结果与当前知识图谱中的已有实体进行跨文件聚类。

输入：

```text
cluster_intermediate.json
```

该文件由 `entity_clustering/cluster_entities.py` 生成，包含新文件内部的：

- Mention
- 文件内 EntityCluster
- Mention 层关系
- 文件内聚类后的 Clustered 关系

输出/写入：

- Neo4j 中的 `File`
- Neo4j 中的 `Chunk`
- Neo4j 中的 `Mention`
- Neo4j 中跨文件合并后的 `EntityCluster`
- Neo4j 中的 `MENTION_*` 关系
- Neo4j 中跨文件合并后的 `CLUSTERED_*` 关系
- MongoDB 中的 `top_event_catalog`

## 2. 核心逻辑

脚本会把新文件内部生成的每个 `EntityCluster` 与当前 Neo4j 中已有的 `EntityCluster` 做保守匹配。

如果判断为同一实体：

```cypher
(:Mention)-[:RESOLVED_TO]->(已有 EntityCluster)
```

并更新已有 EntityCluster 的：

- `source_file_ids`
- `source_file_version_ids`
- `source_file_scopes`
- `mention_ids`
- `aliases`
- `chunk_refs`
- `evidence_json`

如果没有匹配到可信已有实体，则创建新的 `EntityCluster`。

关系导入时，脚本会把文件内关系两端的 cluster id 映射成最终跨文件 cluster id。相同端点、相同关系类型、相同 polarity/certainty 的关系会合并到同一条边上，并累计：

- `source_file_ids`
- `source_file_version_ids`
- `source_file_scopes`
- `involved_chunk_refs`
- `evidence_json`
- `source_relation_ids`

## 3. 跨文件聚类策略

第一版策略偏保守、偏快速，不做全图两两比较，也不强制调用 embedding API。

候选召回条件：

- 实体类型必须一致；
- 不通过硬冲突规则的候选直接过滤；
- `name_score >= 0.70` 或 `neighbor_score >= 0.60`；
- 每个新 EntityCluster 最多保留 Top 10 个候选。

自动合并阈值：

```text
auto_threshold = 0.93
```

其中：

- 名称高度相似时会触发保守 semantic boost；
- 报警码必须完全一致才会合并；
- 存在方向、编号、输入/输出、交流/直流、对象差异等硬冲突时禁止合并。

## 4. 先 dry-run

正式导入前建议先运行 dry-run，不会写 Neo4j/MongoDB，只输出匹配统计和诊断文件。

```powershell
.\.venv\Scripts\python.exe cross_file_entity_clustering.py `
  --env-file .env `
  --intermediate-json entity_clustering\output\luna2000_gnr_ready\cluster_intermediate.json `
  --file-id huawei_luna2000_alarm_reference `
  --file-version-id huawei_luna2000_alarm_reference_v1 `
  --file-name "HUAWEI LUNA2000-(97KWH-200KWH)系列 工商业构网型储能系统 告警参考" `
  --dry-run `
  --diagnostics-jsonl entity_clustering\output\luna2000_gnr_ready\cross_file_diagnostics.jsonl
```

输出中重点看：

- `existing_clusters_scanned`
- `input_clusters`
- `cross_file_merged_clusters`
- `new_clusters`

诊断文件中每一行对应一个新文件 EntityCluster 的匹配决策。

## 5. 正式导入

确认 dry-run 结果后，去掉 `--dry-run`：

```powershell
.\.venv\Scripts\python.exe cross_file_entity_clustering.py `
  --env-file .env `
  --intermediate-json entity_clustering\output\luna2000_gnr_ready\cluster_intermediate.json `
  --file-id huawei_luna2000_alarm_reference `
  --file-version-id huawei_luna2000_alarm_reference_v1 `
  --file-name "HUAWEI LUNA2000-(97KWH-200KWH)系列 工商业构网型储能系统 告警参考" `
  --diagnostics-jsonl entity_clustering\output\luna2000_gnr_ready\cross_file_diagnostics.jsonl
```

默认会同时写 Neo4j 和 MongoDB `top_event_catalog`。

如果只想写 Neo4j，不写 MongoDB：

```powershell
--skip-mongo
```

## 6. 可调参数

```powershell
--auto-threshold 0.93
```

自动合并阈值。跨文件建议保持较高，不要轻易降到同文件聚类的阈值。

```powershell
--candidate-top-k 10
```

每个新 EntityCluster 最多保留几个候选。

```powershell
--candidate-min-name 0.70
```

候选召回的最低名称相似度。

```powershell
--candidate-min-neighbor 0.60
```

候选召回的最低软邻居相似度。

```powershell
--allow-self-loops
```

默认会跳过跨文件合并后产生的 `EntityCluster -> 同一个 EntityCluster` 自环关系。如果确实需要保留自环，可以加这个参数。

## 7. GNR 查询注意

跨文件聚类后，`EntityCluster` 可能由多个文件共同支持，因此不能再假设一个 EntityCluster 只属于一个 `file_version_id`。

GNR 构建故障树时，必须按用户选择的文件版本过滤：

```cypher
MATCH p=(a:EntityCluster)-[r]->(b:EntityCluster)
WHERE r.relation_level = "clustered"
  AND any(fid IN r.source_file_version_ids WHERE fid IN $selected_file_version_ids)
  AND any(fid IN a.source_file_version_ids WHERE fid IN $selected_file_version_ids)
  AND any(fid IN b.source_file_version_ids WHERE fid IN $selected_file_version_ids)
RETURN p;
```

最重要的是：**关系是否可见优先看关系自己的 `source_file_version_ids`。**

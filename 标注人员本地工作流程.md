# 标注人员本地工作流程

本文档说明如何在本地从 `annotation-data-pipeline` 分支开始，完成“文档切块 -> chunks 入库 -> 大模型生成标注草稿 -> 修正 evidence start/end -> Streamlit 人工审核 -> 保存 CSV”的流程。

当前阶段只保留 CSV 产物，不执行 JSONL 转换。

## 1. 建立本地 worktree

在 `smartFTA` 主仓库目录下执行：

```powershell
cd E:\Desktop\study\year3_2026summer\smartFTA
git fetch origin annotation-data-pipeline
git worktree add .\annotation-data-pipeline -b annotation-data-pipeline origin/annotation-data-pipeline
```

如果本地已经存在 `annotation-data-pipeline` 分支，则使用：

```powershell
git worktree add .\annotation-data-pipeline annotation-data-pipeline
```

最终目录应当类似：

```text
smartFTA/
  FTA-KB/
  FTA-GNR/
  annotation-data-pipeline/
```

## 2. 准备环境

### 2.1 新建标注用数据库

在MongoDB新建一个Database，可命名为smart-fta-annotation。


### 2.2 配置 FTA-KB

`FTA-KB` 负责将 PDF / Markdown 文档切成 chunks，并写入 MongoDB。

确认 `FTA-KB/.env` 中 MongoDB 指向本次标注数据库，例如：

```env
MONGO_URI=mongodb://localhost:27017/
MONGO_DB_NAME=smart-fta-annotation
```

### 2.3 配置 annotation-data-pipeline

`annotation-data-pipeline` 负责调用大模型生成标注草稿、修正 offset，并启动人工审核工具。

按.env.example创建 `annotation-data-pipeline/.env`：

```env
MONGO_URI=mongodb://localhost:27017/
MONGO_DB_NAME=smart-fta-annotation
ANNOTATION_CHUNKS_COLLECTION=chunks

OPENAI_API_KEY=你的API_KEY(和其他模块的key一致)
OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
OPENAI_MODEL_NAME=qwen3.6-plus
```

安装依赖：

```powershell
cd ..\annotation-data-pipeline
py -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

## 3. 选择文件并转成 chunks

本阶段每个人先分配到一个文件，我们会在共享文档中写明每个人负责的文件名称与来源。

在命令行单独调用KB模块，利用 KB 模块转 chunks 保存到标注用数据库。
每个待标注文档使用稳定的 `file_id` 和 `file_version_id`。

示例：

```text
file_id: file_sgc_f_gas_turbine_manual
file_version_id: file_sgc_f_gas_turbine_manual_v1
```

推荐使用 KB 模块接口执行。先启动 KB 服务：

```powershell
cd ..\FTA-KB
.\.venv\Scripts\uvicorn.exe main:app --host 0.0.0.0 --port 8020 --reload
```

然后另开一个 PowerShell 窗口调用 KB 模块，将文件转换为 chunks 产物：（具体可以再问自己的AI）

```powershell
$body = @{
  pdf_path = "E:\path\to\manual.pdf"
  pdf_stem = "file_sgc_f_gas_turbine_manual"
  file_version_id = "file_sgc_f_gas_turbine_manual_v1"
  file_name = "F级燃机特殊技术说明文档.pdf"
  output_dir = ".\output"
  chunk_size = 700
  source_type = "manual_document"
  file_format = "pdf"
  chunk_type = "document_section"
  skip_entity = $true
  skip_relation = $true
  sync_to_generate_fta = $false
} | ConvertTo-Json -Depth 10

Invoke-RestMethod `
  -Method Post `
  -Uri "http://127.0.0.1:8020/api/kb/jobs/run" `
  -ContentType "application/json" `
  -Body $body
```

接口会返回 `job_id`。可以用下面的命令查看任务状态：

```powershell
Invoke-RestMethod `
  -Method Get `
  -Uri "http://127.0.0.1:8020/api/kb/jobs/这里替换成job_id"
```

任务成功后，在返回结果或 `FTA-KB/output/` 下找到对应的 chunks 文件，路径通常类似：

```text
FTA-KB/output/file_sgc_f_gas_turbine_manual/file_sgc_f_gas_turbine_manual_v1/file_sgc_f_gas_turbine_manual_chunks.json
```

然后使用 KB 模块的 chunks 导入脚本，将 chunks 保存到标注用 MongoDB：

```powershell
.\.venv\Scripts\python.exe import_chunks.py `
  --file ".\output\file_sgc_f_gas_turbine_manual\file_sgc_f_gas_turbine_manual_v1\file_sgc_f_gas_turbine_manual_chunks.json" `
  --mode append
```

说明：

- `pdf_stem` 会作为本次文档的稳定 `file_id` 使用。
- `file_version_id` 必须显式填写，并且后续生成 AI 标注草稿时要继续使用同一个值。
- `skip_entity = $true`、`skip_relation = $true` 表示本阶段只需要切 chunks，不需要跑原 KB 模块的实体/关系抽取。
- `sync_to_generate_fta = $false` 表示不把这次标注准备数据同步到故障树生成模块。
- `import_chunks.py --mode append` 会把 chunks 写入 `FTA-KB/.env` 中配置的 MongoDB，因此要先确认 `FTA-KB/.env` 指向 `smart-fta-annotation`。
- `chunk_size` 建议先使用 `700` 左右，因为后续大模型标注会采用滑动窗口输入。

如果不通过 KB 服务接口，也可以在 `FTA-KB` 目录下直接调用脚本完成同样的事情：

```powershell
.\.venv\Scripts\python.exe trans_file_to_md.py `
  -i "E:\path\to\manual.pdf" `
  -o .\output `
  --file-id file_sgc_f_gas_turbine_manual `
  --version-no 1

.\.venv\Scripts\python.exe chunk_md.py `
  --input ".\output\file_sgc_f_gas_turbine_manual\file_sgc_f_gas_turbine_manual_v1\xxx.md" `
  --output ".\output\file_sgc_f_gas_turbine_manual\file_sgc_f_gas_turbine_manual_v1\file_sgc_f_gas_turbine_manual_chunks.json" `
  --chunk_size 700 `
  --file_id file_sgc_f_gas_turbine_manual `
  --file_version_id file_sgc_f_gas_turbine_manual_v1 `
  --source_type manual_document `
  --file_format pdf `
  --chunk_type document_section

.\.venv\Scripts\python.exe import_chunks.py `
  --file ".\output\file_sgc_f_gas_turbine_manual\file_sgc_f_gas_turbine_manual_v1\file_sgc_f_gas_turbine_manual_chunks.json" `
  --mode append
```

导入后可以在 MongoDB Compass 中检查：

```text
数据库：smart-fta-annotation
集合：chunks
关键字段：file_id、file_version_id、chunk_id、chapter、content、source_type、chunk_type
```

## 4. 调用大模型生成标注草稿

进入 `annotation-data-pipeline`：

```powershell
cd ..\annotation-data-pipeline
```

先小规模试跑 2-3 个 chunk：

```powershell
.\.venv\Scripts\python.exe generate_ai_annotation_dataset.py `
  --file-version-id file_sgc_f_gas_turbine_manual_v1 `
  --limit 3 `
  --debug-context
```

确认输出正常后，执行完整文件：

```powershell
.\.venv\Scripts\python.exe generate_ai_annotation_dataset.py `
  --file-version-id file_sgc_f_gas_turbine_manual_v1 `
  --output-csv ai_annotation_draft_file_sgc_f_gas_turbine_manual_v1.csv `
  --raw-jsonl raw_ai_annotations_file_sgc_f_gas_turbine_manual_v1.jsonl `
  --debug-context
```

说明：

- CSV 是给人工审核工具使用的草稿数据。
- Raw JSONL 保存每次调用的 prompt、模型原始输出、context chunk、target chunk 等信息，用于排查问题。
- `--debug-context` 会打印每个样本是否使用滑动窗口上下文，以及不用上下文的原因。

### 4.1 PR docx 工单批量处理

如果处理的是服务器数据中的 PR docx 工单，先在 `annotation-data-pipeline/.env` 中配置每个人自己的本地工单根目录：

```env
PR_DOCX_ROOT=E:\path\to\PR_docx
```

然后可以按编号区间批量处理。下面命令会处理编号 `301-302` 的工单，将它们转换为 chunks 并导入 MongoDB：

```powershell
.\.venv\Scripts\python.exe process_pr_work_orders.py `
  --start 301 `
  --end 302
```

脚本会为每个工单生成稳定的 `file_id` 和 `file_version_id`。例如：

```text
301 -> file_version_id=pr_301_sgc_sd_se_153007_pr_001_0_v1
302 -> file_version_id=pr_302_sgc_sd_se_153007_pr_003_0_v1
```

如果要直接完成“转 chunks -> 导入 MongoDB -> 调用大模型标注 -> 合并输出一个 CSV”，加上 `--annotate`：

```powershell
.\.venv\Scripts\python.exe process_pr_work_orders.py `
  --start 1 `
  --end 100 `
  --annotate `
  --output-csv ai_annotation_draft_pr_001_100.csv `
  --raw-jsonl raw_ai_annotations_pr_001_100.jsonl
```

别忘了接下来还需要通过脚本修正 evidence start/end,才能进入到人工审核阶段。

说明：

- PR 工单的 `source_type` 是 `work_order`，`chunk_type` 是 `table_row_summary`。
- 这类工单 chunk 不使用普通 PDF 文档的滑动窗口上下文，模型会逐个工单 chunk 标注。
- 默认导入模式是 `replace`，但它是按 `file_version_id` 局部替换，不会清空其他文件版本的 chunks。
- 如果只想生成 chunks 入库，不消耗 LLM token，不要加 `--annotate`。

PR 工单如果想每一步单独做，按下面走。

#### 4.1.1 只转 chunks 并入库

cd E:\Desktop\study\year3_2026summer\smartFTA\annotation-data-pipeline

.\.venv\Scripts\python.exe process_pr_work_orders.py `
  --start 301 `
  --end 302

这一步会做：
PR docx -> chunks JSON -> 导入 MongoDB chunks

不会调用 LLM。产物在：annotation-data-pipeline/work_order_outputs/

例如：
work_order_outputs/pr_301_sgc_sd_se_153007_pr_001_0/pr_301_sgc_sd_se_153007_pr_001_0_v1/pr_301_sgc_sd_se_153007_pr_001_0_chunks.json

#### 4.1.2 单独调用 LLM 标注

如果只标注 301 和 302：
.\.venv\Scripts\python.exe generate_ai_annotation_dataset.py `
  --file-version-ids "pr_301_sgc_sd_se_153007_pr_001_0_v1;pr_302_sgc_sd_se_153007_pr_003_0_v1" `
  --output-csv ai_annotation_draft_pr_301_302.csv `
  --raw-jsonl raw_ai_annotations_pr_301_302.jsonl `
  --debug-context
如果只标注一个工单：
.\.venv\Scripts\python.exe generate_ai_annotation_dataset.py `
  --file-version-id pr_301_sgc_sd_se_153007_pr_001_0_v1 `
  --output-csv ai_annotation_draft_pr_301.csv `
  --raw-jsonl raw_ai_annotations_pr_301.jsonl `
  --debug-context

#### 4.1.3 单独修正 evidence start/end

.\.venv\Scripts\python.exe validate_and_fix_evidence_offsets.py `
  --input-csv ai_annotation_draft_pr_301_302.csv `
  --output-csv ai_annotation_draft_pr_301_302_offsets_fixed.csv `
  --report-json evidence_offset_report_pr_301_302.json

#### 4.1.4 单独启动人工审核工具

.\.venv\Scripts\streamlit.exe run app.py
打开页面后，左侧选择或上传：
ai_annotation_draft_pr_301_302_offsets_fixed.csv
然后人工审核，保存结果会自动写入新的：
reviewed_annotations_时间戳.csv
所以完整拆开就是：
process_pr_work_orders.py
-> generate_ai_annotation_dataset.py
-> validate_and_fix_evidence_offsets.py
-> streamlit run app.py

## 6. 规范化 evidence 的 start/end

大模型输出的 start/end 可能有偏移，因此需要先运行修正脚本。

```powershell
.\.venv\Scripts\python.exe validate_and_fix_evidence_offsets.py `
  --input-csv ai_annotation_draft_file_sgc_f_gas_turbine_manual_v1.csv `
  --output-csv ai_annotation_draft_file_sgc_f_gas_turbine_manual_v1_offsets_fixed.csv `
  --report-json evidence_offset_report_file_sgc_f_gas_turbine_manual_v1.json
```

输出文件：

```text
ai_annotation_draft_file_sgc_f_gas_turbine_manual_v1_offsets_fixed.csv
evidence_offset_report_file_sgc_f_gas_turbine_manual_v1.json
```

说明：

- 如果 evidence 文本能在对应原文中找到，脚本会自动替换成正确 start/end。
- 如果找不到，会将 start/end 置空，并计入错误统计。
- 人工审核时优先导入 `_offsets_fixed.csv`。

## 7. 启动 Streamlit 人工审核工具

```powershell
.\.venv\Scripts\streamlit.exe run app.py
```

打开浏览器中的 Streamlit 页面。

左侧栏有两种加载方式：

1. 上传 CSV；
2. 从 `annotation-data-pipeline/` 当前目录选择 CSV。

推荐导入：

```text
ai_annotation_draft_file_sgc_f_gas_turbine_manual_v1_offsets_fixed.csv
```

## 8. 人工审核要求

每个样本对应一个滑动窗口：

- `context_text`：上一个符合条件的上下文 chunk；没有上下文时为空。
- `text`：当前 target chunk，是本样本的主要标注对象。

审核原则：

- 只标注当前 `text` 中出现的实体和关系。
- 如果关系证据跨越 `context_text` 和 `text`，可以保留跨 chunk evidence。
- 完全只出现在 `context_text` 中，且与当前 `text` 无关的实体/关系，不需要标注。
- 实体的原文定位统一写在 `evidence` 数组中；实体本身不单独保存 start/end。
- 点击实体或关系行时，左侧原文会按 evidence 的 start/end 高亮，便于检查 offset 是否准确。
- `logic_groups` 表示同一个逻辑门连接的一组事件，例如多个下层事件共同导致一个上层事件。

审核时建议逐条检查：

1. 实体类型是否正确；
2. 实体 `mention` 是否概括得合理；
3. `normalized_name` 是否适合作为规范名称；
4. 关系方向是否正确；
5. 关系类型是否属于规定类型；
6. evidence 是否真的支持该实体或关系；
7. start/end 高亮位置是否准确；
8. 状态最终是否标记为 `reviewed`。

## 9. 保存与续审规则

审核工具不会覆盖固定的 `reviewed_annotations.csv`。

每次加载一个 CSV 后，工具会自动创建新的时间戳保存文件：

```text
reviewed_annotations_YYYYMMDD_HHMMSS.csv
```

保存规则：

- 点击“保存当前样本”：保存当前样本修改到本次时间戳 CSV。
- 点击“标注为已审核”：将当前样本状态改为 `reviewed`，并保存到本次时间戳 CSV。
- 点击“上一个样本 / 下一个样本”：会先自动保存当前样本，再切换。
- 页面意外关闭后，已触发保存的内容仍保留在本次时间戳 CSV 中。

多天续审规则：

1. 第一天导入 `_offsets_fixed.csv`，审核工具保存为 `reviewed_annotations_时间戳A.csv`。
2. 第二天继续时，不再导入原始 draft CSV，而是导入昨天最新的 `reviewed_annotations_时间戳A.csv`。
3. 第二天审核工具会继续保存为新的 `reviewed_annotations_时间戳B.csv`。
4. 后续每天都导入上一轮最新的 reviewed 时间戳 CSV。
5. 全部样本都变成 `reviewed` 后，再手动将最终文件重命名为：

```text
reviewed_annotations.csv
```

## 10. 常见问题

### 加载 CSV 后生成了一个 reviewed 时间戳文件，正常吗？

正常。工具会为本次审核会话准备一个新的保存目标。后续修改都会写入这个文件。

### 为什么不要每天重新导入原始 draft CSV？

原始 draft CSV 不包含前一天人工修改的结果。续审时必须导入上一轮最新的 `reviewed_annotations_时间戳.csv`。

### 为什么 start/end 仍然可能为空？

如果 evidence 文本无法在 `context_text` 或 `text` 中精确找到，修正脚本会将 start/end 置空。人工审核时可以重新选择原文片段，并用 evidence quick add 补充。

### 可以直接修改 CSV 吗？

可以，但不推荐。优先使用 Streamlit 工具审核，因为它能显示原文、高亮 evidence，并减少 JSON 字段手动编辑错误。

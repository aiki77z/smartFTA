故障树自动生成模块技术说明文档
1. 模块定位与概述
本模块是“基于知识的工业设备故障树智能生成与辅助构建系统”的核心子任务，旨在解决工业场景下故障树分析（FTA）构建成本高、依赖专家经验、知识复用率低等痛点。
不同于传统的一次性大模型生成方案，本模块构建了一个包含知识检索、要素抽取、结构生成、逻辑校验、差异分析与闭合学习的完整工程闭环。其核心价值在于不仅能从原始工业文档自动生成标准化的故障树，更能够通过“专家修正反馈机制”，将人工经验沉淀为结构化知识，实现生成质量的持续进化。
2. 技术栈架构
•	后端框架：FastAPI (异步高性能 Web 框架)
•	数据存储：MongoDB (非结构化 JSON 存储，支持灵活的版本管理)
•	核心引擎：OpenAI 兼容接口 (对接 DeepSeek 模型)
•	自然语言处理：jieba (关键词提取)、大模型语义解析
•	运行环境：Python 3.9+
3. 核心接口定义
3.1 单树生成接口 (/api/tree/generate)
功能：当前版本的主入口已升级为“自然语言输入 + 异步任务生成”模式。前端仅需提交 prompt，后端会先调用 parse_user_prompt() 自动解析顶事件与额外要求，再进入任务调度流程。
新增能力：该接口支持三种处理结果。若系统已存在同一顶事件、同一归一化名称或别名命中的历史故障树，则直接复用已有结果并返回 mode=reuse；若当前不存在可复用结果，则创建单生成任务并返回 mode=queued、job_id、item_id、status 与 progress；若该顶事件已经处于批量任务队列中且仍为待执行状态，则系统会优先抢占该任务并启动 dedicated_worker 专用线程加速生成，以满足“后台批量执行、前台优先响应用户指定顶事件”的业务需求。
输出：接口返回值根据场景不同，包含 mode、job_id、item_id、tree_id、version、progress、parsed_prompt、tree_data 等字段。前端可根据 mode 判断是直接展示已存在故障树，还是进入任务轮询流程。
3.2 批量生成接口 (/api/batch/generate-all)
功能：该接口用于一次性批量生成当前知识库中可识别的全部顶事件故障树。系统会先读取 chunks 集合中的全部知识片段，再调用 discover_top_events_from_chunks() 自动识别顶事件候选，并统一写入或合并到 top_event_catalog 顶事件目录中。
关键逻辑：批量生成前，系统会自动检查每个顶事件是否已经存在可复用故障树，或是否已有进行中的任务项；只有“既没有历史结果、也没有活动任务”的顶事件才会进入新的批量任务队列，从而避免重复生成。
输出：返回 job_id、status、discovered_total、catalog_total、existing_count、active_count、queued_count 以及 queued_item_ids 等信息，便于前端展示批任务总体情况。
3.3 批任务状态查询接口 (/api/batch/job/{job_id}、/api/batch/job-item/{item_id})
功能：批任务查询接口用于配合异步生成模式展示任务进度。其中，GET /api/batch/job/{job_id} 用于查看一个批量任务的整体状态与全部任务项信息；GET /api/batch/job-item/{item_id} 用于查看某一个任务项的实时执行状态。
输出：任务状态信息包含 status、progress、stage、message、tree_id、error、duration_seconds 等字段，可支撑前端实现进度条、阶段提示、失败原因显示和耗时统计。
3.4 顶事件目录接口 (/api/catalog/top-events)
功能：新增的顶事件目录接口用于返回系统当前维护的标准化顶事件列表。目录数据来源于 top_event_catalog 集合，系统会对顶事件进行归一化命名、别名合并以及来源 chunk 记录管理。
作用：该接口可用于前端构建顶事件候选列表、搜索下拉框、批量生成范围展示和标准化顶事件管理页面，是当前批量生成和结果复用机制的重要支撑接口。
3.5 专家修正与学习接口 (/api/tree/{tree_id}/save)
功能：保存专家编辑后的故障树，并将其写入新的版本记录。
新增能力：保存前，系统会先执行结构校验；若不通过则拒绝保存。若前端未填写 description，或仅填写默认值“手动修改”，后端会自动调用 generate_change_description() 根据前后版本差异生成简短修改说明。若上一版本为 AI 生成版本，则系统会自动执行差异分析 analyze_and_store()，并将新增、删除、改名、逻辑门修改、连接增删等修正经验写入 corrections 集合。
输出：返回 success、version 与 learned_count。其中 learned_count 表示本次保存沉淀下来的修正经验条数，可直接体现“专家修正反馈 -> 系统持续学习”的闭环效果。
3.6 故障树获取与版本管理接口 (/api/tree/{tree_id}、/api/tree/{tree_id}/version/{version}、/api/tree/{tree_id}/history、/api/tree/{tree_id}/rollback/{target_version})
功能：该组接口用于支撑生成结果查询、历史版本追溯和版本恢复。GET /api/tree/{tree_id} 获取当前版本故障树；GET /api/tree/{tree_id}/version/{version} 获取指定版本；GET /api/tree/{tree_id}/history 返回该树的版本历史；POST /api/tree/{tree_id}/rollback/{target_version} 则可将当前树回滚到指定历史版本。
作用：该能力保证了 AI 初稿、专家修订稿与回滚版本都可被保留和对比，便于前端实现历史查看、差异对照与恢复操作。
3.7 校验接口 (/api/tree/validate、/api/tree/validate/semantic)
功能：系统当前提供两类校验接口。POST /api/tree/validate 用于执行完整校验流程，返回结构校验与语义校验的综合结果；POST /api/tree/validate/semantic 用于只做语义层检查，重点评估逻辑门是否合理、因果关系是否成立、底事件是否足够具体，以及是否存在明显遗漏的故障路径。
作用：完整校验接口适合在保存前对整棵树做统一检测；独立语义校验接口则更适合前端在专家编辑完成后做快速逻辑复核，也便于调试阶段定位“结构错误”与“语义问题”的区别。
3.8 溯源与修正查看接口 (/api/chunk/{chunk_id}、/api/corrections/{tree_id})
功能：GET /api/chunk/{chunk_id} 用于按 chunk_id 返回知识片段详情，支持前端从故障树节点回看其知识来源；GET /api/corrections/{tree_id} 用于查看当前故障树沉淀下来的全部修正记录，并按 created_at 倒序返回。
作用：这组接口增强了系统的可解释性与可学习性，便于展示“生成依据”和“学习痕迹”。
3.9 服务导航接口 (/)
功能：根接口用于返回当前服务运行状态、文档入口以及典型调用示例。当前返回内容包括 docs、generate_input_example 和 batch_endpoint，可作为后端服务的轻量级导航入口。
3.10 可选兼容接口 (/validate-fault-tree、/export-fault-tree-image)
说明：若部署目录中存在 validator-service 子服务，后端会在同一端口下额外挂载 /validate-fault-tree 与 /export-fault-tree-image 两个兼容接口，用于复用原有的校验与图像导出能力。该能力为可选挂载，不影响主流程接口运行。
4. 故障树智能生成全链路流程
系统遵循“知识驱动 + 逻辑约束 + 经验修复”的原则，共分为六个核心步骤：
Step 1：精准知识检索
系统利用 jieba 提取顶事件关键词，在 MongoDB 的 chunks 知识库中进行匹配，筛选出与当前故障主题相关的非结构化工业知识片段，作为大模型推理的“外部大脑”。
Step 2：故障要素抽取 (LLM#1)
调用大模型从检索到的知识中提取标准化 FTA 元素：
•	故障事件（顶事件、中间事件、底事件）
•	逻辑门类型（与门、或门）
•	因果传递路径
•	工程化属性（触发规则、排查方法、故障概率）
Step 3：标准化草稿生成 (LLM#2)
依据 FTA 建模规范，将抽取的要素组装为包含 nodeList 和 linkList 的标准化 JSON 结构。规定“原因节点指向结果节点”，确保数据结构可直接被前端图形引擎渲染。
Step 4：自动化结构校验与重试
生成后立即触发校验引擎。针对“逻辑门缺失”、“孤立节点”、“环路因果”、“底事件含子节点”等 8 类常见错误进行自动检测。若校验失败，系统携带错误日志请求模型重修，最大支持 3 轮自动重试。
Step 5：基于历史修正的定向修复 (LLM#3) —— 核心创新点
系统检索 corrections 库中与当前树节点名称、故障类型高度相关的历史修正记录。LLM#3 将这些专家经验作为“定向修复指令”，对草稿树进行局部微调，确保生成结果符合该企业的特定业务术语和专家习惯。
Step 6：最终语义与逻辑确认
在结构合规的基础上，由大模型进行语义层面扫描，评估逻辑门选用的合理性、因果链条的严密性以及底事件是否已拆解至最小可排查单元。




5. 数据模型设计
集合名称	核心字段	业务作用
fault_trees	top_event, current_version, status	记录故障树的主索引与当前状态
fault_tree_versions	tree_data (JSON), is_ai_generated, editor	存储多版本数据，支持历史追溯与回滚
chunks	content, source_doc, key_words	存储工业手册切片，提供生成依据与溯源
corrections	node_name, correction_type, expert_detail	沉淀专家经验，作为系统持续学习的知识单元



6. 三大核心创新点总结
创新点 1：分阶段协同生成链路
将复杂的生成任务拆解为“抽取 -> 组装 -> 修复”三个阶段。LLM#1 关注知识准确性，LLM#2 关注结构合规性，LLM#3 关注经验对齐。这种“流水线”式设计显著提升了系统在处理复杂工业长文档时的稳定性。
创新点 2：基于专家修正的闭环持续学习机制
系统不仅是一个生成工具，更是一个“学习型助手”。通过自动分析 AI 与专家的差异，将零散的人工修改转化为可检索的规则。这一机制使得系统能够随着使用时间的增加，逐渐对齐特定领域专家的思维模式。
创新点 3：节点级加权检索与最小编辑修复策略
在经验迁移过程中，系统采用加权算法优先匹配高频、高价值的修正建议，并严格约束修复模型仅针对相关节点进行“手术刀式”微调，有效避免了历史经验对新生成场景的干扰。

7. 图谱接入与召回增强

7.1 第一阶段：把图谱作为召回增强器
系统已接入 Neo4j 图谱检索层。当前版本不会让图谱直接替代故障树生成，而是先把图谱作为高精度 chunks 召回器，用于增强原有“关键词 + 实体索引”检索链路。

当前单树生成时的知识召回顺序为：
1. 图谱召回：先从 `(:Entity:FaultPhenomenon)` 顶事件节点出发，查询与该顶事件直接相关的 chunks。
2. 实体反向索引召回：再查询 MongoDB 中的 `entity_reverse_index`，补充实体级命中 chunks。
3. 文本关键词兜底：最后对 `chunk_name`、`content`、`chapter`、`section`、`subsection` 做关键词匹配。

因此，当前主链路可以概括为：
`图谱召回 -> 实体索引召回 -> 关键词兜底 -> 要素抽取 -> 草稿树生成 -> 校验 -> 修复`

7.2 generate-all 的顶事件来源升级
`POST /api/batch/generate-all` 当前不再只依赖 chunks 或关键词抽取顶事件，而是按以下优先级发现顶事件：
1. 优先读取 Neo4j 中的 `FaultPhenomenon` 节点，作为顶事件主来源。
2. 若图谱不可用或未命中，则回退到 MongoDB 中的 `entity_reverse_index`。
3. 若实体索引也不可用，则回退到 chunks 中的实体抽取与规则抽取结果。

这样做的目的，是让顶事件列表尽量来源于结构化知识，而不是只靠正文中的局部词面命中。

7.3 FaultPhenomenon 顶事件过滤规则
虽然图谱中的 `FaultPhenomenon` 节点可作为顶事件主来源，但并不是所有节点都适合直接进入批量生成列表。系统当前会过滤以下明显不适合作为顶事件的节点：
- 纯故障码或报警码，如 `F01640`、`A01631`
- 纯十六进制值、原因码或状态码，如 `000B`、`000F`、`0013`、`0065`、`008B`、`008C`
- 明显属于 SI / SIP 编号类节点，如 `SI P1`、`SIP1`
- 明显不是故障现象本身的编号式节点
- 明显偏向配置状态、保存状态、章节标题的非故障现象节点

保留策略的核心原则是：优先保留“可被视为故障现象、可作为故障树顶事件的节点”，过滤明显的编码值、编号值和非现象类节点。

7.4 第一阶段图谱查询策略
第一阶段的图谱召回主要围绕“顶事件 -> 直接原因 -> chunks”展开，目标是优先获得高精度上下文。

基础查询逻辑为：
1. 查询顶事件自身出现过的 chunks：
`(top:Entity:FaultPhenomenon)-[:MENTIONED_IN]->(c:Chunk)`
2. 查询直接原因：
`(cause:Entity)-[:RELATION {relation_type:'故障触发'}]->(top:Entity:FaultPhenomenon)`
3. 查询这些直接原因出现过的 chunks：
`(cause)-[:MENTIONED_IN]->(c:Chunk)`

系统会将顶事件自身 chunks 与直接原因 chunks 合并，并与实体索引、关键词结果一起做去重和排序，最终选出送入 LLM 的知识上下文。

7.5 第一阶段工程落点
第一阶段图谱接入的主要工程位置如下：
- `graph_retriever.py`：负责 Neo4j 查询，包括顶事件列表读取、图谱召回、局部子图抽取。
- `generator.py`：在生成前串联“图谱召回 + 实体索引 + 关键词兜底”的知识检索流程。
- `main.py`：让 `/api/batch/generate-all` 与 `/api/batch/preview-top-events` 复用新的顶事件发现逻辑。
- `config.py`：新增图谱配置项，包括 `ENABLE_GRAPH_RETRIEVAL`、`NEO4J_URI`、`NEO4J_USER`、`NEO4J_PASSWORD`、`NEO4J_DATABASE`。

7.6 第二阶段：把图谱当故障树草稿生成器
系统会在单树生成前，从图谱中抽取顶事件相关的局部子图，并把这份子图转换为“候选树草稿”，再与 chunks 一起交给现有生成链路。

第二阶段的目标不是让图谱替代 LLM，而是让图谱为 LLM 提供结构先验，减少“从零造树”的随机性。

7.7 第二阶段执行顺序
当前第二阶段的执行顺序如下：
1. 顶事件完成标准化后，先执行第一阶段图谱召回，获得相关 chunks。
2. 调用 `build_graph_draft_candidates()`，从 `FaultPhenomenon` 顶事件出发，抽取满足 `故障触发` 的直接原因节点。
3. 对每个直接原因节点做一跳扩展，扩展范围包括 `组成关系`、`依赖关系`、`通讯连接`、`参数配置`、`功能支持`、`故障处理` 等关系。
4. 将局部子图中的节点映射为候选故障树节点与候选关系。
5. 将“图谱候选草稿 + chunks”一起送入 `extract_fault_elements()` 与 `build_fault_tree()`，由现有 LLM 生成模块完成规范化、补全和修复。

7.8 图谱节点到故障树节点的映射规则
当前系统采用如下映射规则：
- `FaultPhenomenon`：优先映射为故障树事件节点；若还能继续展开，则优先作为 `intermediate_event`，否则作为 `basic_event`。
- `Component`：转写为“组件故障 / 组件异常”类事件，例如“功率单元故障”“编码器异常”。
- `Parameter`：转写为“参数异常 / 参数不匹配 / 参数未保存”类原因，通常更接近 `basic_event`。
- `System`：转写为“通讯异常 / 系统异常”类事件，例如“PROFIBUS 通讯异常”“DRIVE-CLiQ 通讯异常”。
- `Method / Tool`：不直接进入树结构，优先写入 `investigateMethod` 或 `description`。
- `Chunk`：不进入树节点，只作为 `documents` 溯源证据。

7.9 第二阶段的工程落点
第二阶段主要落在以下位置：
- `graph_retriever.py`
  - `build_graph_draft_candidates()`：从顶事件出发抽取局部子图，并生成 `direct_causes`、`expanded_nodes`、`draft_relations` 与 `investigate_methods`
  - `_map_entity_to_tree_event()`：将图谱节点映射为候选故障树节点
- `generator.py`
  - `generate_fault_tree()`：在原有召回链路基础上，新增图谱草稿构建步骤
  - `extract_fault_elements()`：新增 `graph_context` 输入，把图谱候选草稿作为 LLM#1 的提取约束
  - `build_fault_tree()`：新增 `graph_context` 输入，把图谱候选草稿作为 LLM#2 的结构约束

7.10 第二阶段对主流程的影响
第二阶段没有推翻原有主流程，仍然保留：
`chunks召回 -> 要素抽取 -> 草稿树生成 -> 结构校验 -> 历史修正 -> 最终校验`

区别在于：
- 以前：LLM 主要依赖 chunks，从零开始推断树结构。
- 现在：LLM 会先看到一版图谱候选草稿，再结合 chunks 做结构确认、节点补全和文案规范化。

因此，图谱在系统中的角色是“结构先验提供者”，而不是“最终树结构的唯一决定者”。

7.11 图谱召回升级：三层召回
当前图谱召回已从单层升级为三层，以解决只查一层时“精度高但覆盖不足”的问题。

当前三层召回如下：
1. Layer A：顶事件自身 chunks + 一跳“故障触发”直接原因 chunks
2. Layer B：二跳“故障触发”原因链 chunks
3. Layer C：一跳结构支撑 chunks，仅保留组件 / 系统 / 参数类支撑节点

三层召回的目标分别是：
- Layer A：保证与顶事件最直接相关的知识命中
- Layer B：补充更深一层的因果链，帮助树自然向下展开
- Layer C：补充组件、系统、参数等支撑性信息，使抽象原因更容易被细化

7.12 路径保真机制
为保证图谱召回结果可解释，系统不仅返回 `chunk_id`，还会为每个命中的 chunk 保留“为什么命中”的路径信息。当前每个 chunk trace 包含以下字段：
- `source_layer`：来源层级，取值为 `A`、`B`、`C`
- `matched_entity`：触发本次命中的关键实体
- `path`：从顶事件到命中实体的路径节点序列
- `path_relations`：路径上对应的关系序列
- `score`：该路径对 chunk 的贡献分数
- `evidences`：该 chunk 的全部候选证据路径

系统会对同一 chunk 的多条命中路径做聚合，保留一条主证据路径，并把完整证据集合保存在 `evidences` 中，便于后续调试与前端解释展示。

7.13 分层优先级与打分策略
当前图谱召回采用“分层优先级 + 关系加权”的打分方式，避免无限扩层带来的噪声问题。整体原则如下：
- Layer A 权重最高：顶事件自身 chunks 分数最高，一跳直接原因次之
- Layer B 次高：用于补充第二层主因果链
- Layer C 为中等权重：仅用于补充结构支撑，不压过主因果链

在 Layer C 内部，不同关系会进一步区分权重，例如：
- `组成关系`
- `依赖关系`
- `通讯连接`
- `参数配置`

同时，系统只保留 `Component`、`System`、`Parameter` 三类支撑节点，避免方法类、工具类节点过早污染主因果召回结果。

7.14 本次图谱召回升级的工程落点
本次三层召回与路径保真升级主要落在以下位置：
- `graph_retriever.py`
  - `search_graph_related_chunks()`：执行 A / B / C 三层图谱召回，并返回 `chunk_traces` 与 `layer_counts`
  - `_merge_chunk_trace_buckets()`：对同一 chunk 的多条命中路径做聚合
  - `_score_support_relation()`：为 Layer C 不同关系类型分配分数
- `generator.py`
  - `_format_chunks()`：把 chunk 的 `retrieval_trace` 一并组织进 LLM 上下文
  - `generate_fault_tree()`：把 `chunk_traces` 写入 `tree_data.retrieval.graph.chunk_traces`

7.15 图谱召回调试接口
为便于在不生成故障树的情况下单独查看图谱召回结果，系统新增调试接口：
`POST /api/debug/graph-recall`

请求体支持两种方式：
1. 直接传顶事件
```json
{
  "top_event": "输出异常",
  "limit": 12
}
```
2. 传自然语言 prompt，由后端先解析顶事件
```json
{
  "prompt": "请分析输出异常的图谱召回结果",
  "limit": 12
}
```

返回内容包括：
- `query`：本次调试请求的顶事件、归一化结果、alias 与解析结果
- `catalog_entry`：若命中 `top_event_catalog`，则返回对应目录项
- `graph_recall.layer_counts`：A / B / C 三层召回统计
- `graph_recall.chunk_traces`：聚合后的路径保真结果
- `graph_recall.chunks`：每个命中 chunk 的基础信息与 `retrieval_trace`
- `graph_draft`：当前顶事件对应的图谱候选草稿摘要

该接口不会创建任务、不会生成故障树、也不会写入版本数据，仅用于调试图谱召回质量和图谱草稿构造结果。

7.16 顶事件预览接口
为支持“只查看顶事件列表、不启动 batch 生成任务”的场景，系统新增：
`POST /api/batch/preview-top-events`

请求体示例：
```json
{
  "limit": 200
}
```

接口行为如下：
- 复用与 `POST /api/batch/generate-all` 相同的顶事件发现逻辑
- 只返回发现结果，不创建任务、不写入 job、不触发故障树生成
- 可用于前端先预览当前知识库可识别的顶事件列表

返回内容包括：
- `discovery_source`：顶事件发现来源（`neo4j_fault_phenomenon` / `entity_reverse_index` / `chunks`）
- `total`：本次发现到的顶事件总数
- `returned`：本次返回条数
- `items`：顶事件列表。每项包含 `name`、`aliases`、`source_chunk_ids`、`normalized_candidates`、`catalog_hit`、`catalog_name`

该接口适合在真正调用 `generate-all` 前先做预览确认，避免因为顶事件抽取规则、图谱节点命名或过滤策略问题而直接启动大批量生成。

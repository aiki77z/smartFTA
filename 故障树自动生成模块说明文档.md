# 故障树自动生成模块说明

## 1. 当前版本目标

当前版本的生成主骨架已经切换为“图谱驱动”模式，整体目标是：

1. 解析用户 `prompt`
2. 直接在图谱中匹配顶事件节点
3. 围绕顶事件扩展局部子图
4. 收集局部子图节点与边直接关联的 chunks
5. 将“局部子图骨架 + 精简证据 chunks”交给大模型生成最终故障树 JSON
6. 继续沿用 corrections 修复与 validator 校验机制

## 2. 图谱结构约定

### 2.1 节点类型

当前图谱只保留两类节点：

- 故障现象与报警节点
- AND 逻辑节点

默认逻辑为 OR，不单独创建 OR 节点。

### 2.2 故障现象节点属性

故障现象节点采用平铺属性结构，典型字段如下：

- `name`
- `normalized_name`
- `entity_type`
- `description`
- `errorLevel`
- `priority`
- `probability`
- `showProbability`
- `rule`
- `investigateMethod`
- `documents`
- `source_chunk_ids`
- `support_count`

其中：

- `documents` 仅保存最小溯源结构，例如 `[{ "chunk_id": "25" }]`
- `source_chunk_ids` 保存同一批 chunk_id 的纯列表
- `support_count` 表示该节点被多少个 chunk 支撑

### 2.3 关系边

图谱关系统一使用 `relation_type = 触发`。

关系边建议同时保留：

- `chunk_id`
- `source_chunk_ids`
- `support_count`

以便后续按边收集证据。

## 3. generate 接口内部流程

`POST /api/tree/generate` 的外部协议不变，仍然只接收 `prompt`。

当前内部流程已经调整为：

1. `parse_user_prompt`
2. `match_top_event_from_graph`
3. `expand_local_fault_subgraph`
4. `collect_subgraph_chunks`
5. `build_fault_tree_from_subgraph_and_chunks`
6. `get_relevant_corrections`
7. `repair_fault_tree`
8. `validate_full`

### 3.1 顶事件匹配

顶事件匹配直接访问图谱节点，不再依赖：

- `chunks`
- `entity_reverse_index`

匹配顺序为：

1. `name` 精确匹配
2. `normalized_name` 精确匹配
3. 去空格归一化匹配
4. 文本包含关系匹配

如果输入的是报码，系统会优先尝试跳转到对应的可读故障现象节点。

### 3.2 局部子图扩展

系统从顶事件节点出发，沿“原因 -> 结果”的反方向展开局部原因子图。

当前控制参数：

- 最大深度：`GRAPH_TREE_MAX_DEPTH`
- 最大节点数：`GRAPH_TREE_MAX_NODES`

子图输出包含：

- `root`
- `nodes`
- `edges`
- `gate_groups`
- `support_chunk_ids`

### 3.3 子图关联 chunks 召回

系统只召回局部子图直接关联的 chunks，禁止再做全库关键词扩散召回。

召回来源包括：

- 节点 `documents`
- 节点 `source_chunk_ids`
- 边上的 `chunk_id`
- 边上的 `source_chunk_ids`

### 3.4 LLM 生成树

LLM 不再负责从 chunks 中“发明结构”，只负责：

- 根据局部子图骨架输出标准 `nodeList` / `linkList`
- 用证据 chunks 补全事件字段
- 保持节点 `graphNodeId` / `kg_key`

系统会在后处理阶段统一补全：

- `event.documents`
- `graphNodeId`
- `kg_key`
- 缺省事件字段

## 4. generate-all 接口

`POST /api/batch/generate-all` 已改为基于图谱候选顶事件列表生成。

顶事件候选来源：

- `list_graph_top_event_candidates()`

不再从：

- `chunks`
- `entity_reverse_index`

重新推断顶事件。

## 5. 顶事件列表接口

新增接口：

- `GET /api/top-events`

用于前端直接获取当前图谱候选顶事件目录。

每项最少包含：

- `graph_node_id`
- `name`
- `normalized_name`
- `support_count`
- `source_chunk_ids`
- `documents`

## 6. 保存与回写

`POST /api/tree/{tree_id}/save` 当前支持两类处理：

### 6.1 属性修改

如果修改的是以下字段：

- `description`
- `errorLevel`
- `priority`
- `probability`
- `showProbability`
- `rule`
- `investigateMethod`

系统会根据节点上的 `graphNodeId` / `kg_key` 回写到图谱节点属性。

### 6.2 名称与结构修改

以下修改暂不回写图谱主结构：

- 节点名称修改
- 新增节点
- 删除节点
- gate 修改
- link 修改

这些改动仍然走 corrections 机制。

## 7. 输出格式与 validator

最终输出仍然保持原有前端兼容格式：

- `nodeList`
- `linkList`

节点类型仍使用：

- `top_event`
- `intermediate_event`
- `basic_event`

`AND` 节点不会直接暴露给前端，而是在“子图转树”阶段折叠为父节点的 `gate = "AND"`。

## 8. 当前实现边界

当前版本先只考虑：

- 单一知识源
- 单一图谱
- 图谱主骨架生成
- 局部子图证据召回
- 顶事件目录来自图谱
- 属性修改回写图谱
- corrections 与 validator 兼容

暂不处理：

- 多知识源切换
- embedding 语义匹配顶事件
- 图谱结构自动回写
- 多版本图谱合并

# 故障树自动生成模块说明文档

## 1. 模块定位

本模块面向“基于知识的工业设备故障树智能生成与辅助构建系统开发”赛题中的核心子任务，负责根据已导入的工业知识数据和用户指定的顶事件，按照故障树分析（FTA）规范，自动构建完整的故障树结构，并为后续专家可视化编辑、逻辑校验与持续优化提供标准化输出。

当前代码实现的重点，不是单纯调用一次大模型直接出图，而是形成了一个较完整的闭环：

知识检索 -> 故障要素抽取 -> 故障树草稿生成 -> 结构校验重试 -> 基于历史修正的定向修复 -> 最终逻辑校验 -> 版本化保存

其中，“基于用户修改优化生成能力”的能力已经具备初步实现基础，是本模块最有代表性的创新点。

## 2. 当前技术栈

- 后端框架：FastAPI
- 数据存储：MongoDB
- 大模型调用：OpenAI Python SDK 兼容接口
- 中文关键词提取：jieba
- 运行依赖：`fastapi`、`uvicorn`、`pymongo`、`openai`、`python-dotenv`、`pydantic`、`jieba`

模型配置通过环境变量管理，默认兼容 DeepSeek 风格接口。

## 3. 当前接口实现

### 3.1 生成接口

接口：`POST /api/tree/generate`

实现位置： [main.py]

请求参数：

- `top_event`：用户指定的顶事件
- `requirements`：用户补充的生成要求

处理流程：

1. 创建一棵新的故障树主记录；
2. 调用 `generate_fault_tree(top_event, requirements)` 执行完整生成流程；
3. 将结果作为 AI 初始版本写入版本表；
4. 返回 `tree_id`、`version` 与 `tree_data`。

该接口对应自动生成模块的主入口，是你负责部分最直接的对外能力。

### 3.2 故障树保存接口

接口：`POST /api/tree/{tree_id}/save`

实现位置： [main.py]

请求参数：

- `tree_data`：专家修改后的故障树
- `editor`：编辑人，默认“专家”
- `description`：修改说明

处理流程：

1. 对专家编辑后的树执行结构校验；
2. 若校验通过，则保存为新版本；
3. 若上一版本为 AI 生成版本，则自动触发 AI 树与专家树的差异分析；
4. 将差异结果沉淀到 `corrections` 集合中，供下一次生成时复用。

这个接口虽然属于“专家辅助构建”阶段，但它直接承担了你所说的“根据用户修改优化生成能力”的学习入口，是当前创新闭环的关键支撑。

### 3.3 故障树获取接口

接口与实现位置：

- `GET /api/tree/{tree_id}`，见 [main.py]
- `GET /api/tree/{tree_id}/version/{version}`，见 [main.py]
- `GET /api/tree/{tree_id}/history`，见 [main.py]

作用：

- 获取当前版本故障树；
- 获取指定版本故障树；
- 获取版本历史，支持回溯 AI 生成结果与专家修改结果。

### 3.4 回滚接口

接口：`POST /api/tree/{tree_id}/rollback/{target_version}`

实现位置： [main.py]

作用：

- 支持将当前树回滚到某一历史版本；
- 便于专家对不同生成结果进行对比与恢复。

### 3.5 校验接口

接口：`POST /api/tree/validate`

实现位置： [main.py]

作用：

- 对单棵树做独立校验；
- 便于前端在保存前先做快速检测；
- 也可作为调试与测试接口使用。

### 3.6 溯源与修正查看接口

接口与实现位置：

- `GET /api/chunk/{chunk_id}`，见 [main.py]
- `GET /api/corrections/{tree_id}`，见 [main.py]

作用：

- 支持前端按 `chunk_id` 查看知识来源，增强故障树可解释性；
- 支持查看当前树积累的修正记录，便于展示“系统在持续学习”。

## 4. 自动生成模块的核心实现

### 4.1 总体入口

核心函数：`generate_fault_tree(top_event, requirements)`

实现位置： [generator.py]

该函数是自动生成故障树的统一编排入口，内部串联了知识检索、故障要素抽取、草稿生成、校验重试、历史修正检索、定向修复和最终校验。

### 4.2 Step 1：知识检索

相关函数：

- `_extract_keywords()`，见 [generator.py]
- `search_chunks_by_keywords()`，见 [database.py]

实现说明：

- 先对顶事件进行关键词提取；
- 再在 MongoDB 的 `chunks` 集合中按 `key_word` 做匹配检索；
- 返回与当前故障主题相关的知识片段，作为后续大模型推理的知识上下文。

这一步实现了“基于知识驱动生成”，避免模型完全脱离工业文档凭空生成。

### 4.3 Step 2：故障要素抽取

核心函数：`extract_fault_elements()`

实现位置： [generator.py]

实现说明：

- 调用 LLM#1，从检索到的工业知识片段中提取：
  - 故障事件；
  - 事件类型；
  - 父子因果关系；
  - 逻辑门信息；
  - 触发规则；
  - 排查方法等信息。
- 输出结构化 JSON，包括 `events` 与 `relations` 两部分。

这一层相当于把非结构化知识转换成可用于 FTA 建模的中间语义表示，是自动生成的基础。

### 4.4 Step 3：故障树草稿生成

核心函数：`build_fault_tree()`

实现位置： [generator.py]

实现说明：

- 调用 LLM#2，根据故障要素和用户要求生成标准化故障树 JSON；
- 输出格式固定为：
  - `nodeList`
  - `linkList`
- 明确约束节点类型、逻辑门、事件字段、ID 规则、文档溯源字段等内容；
- 将 `sourceId -> targetId` 定义为“原因节点指向结果节点”，符合故障树自底向上的因果表达。

当前输出数据结构已经能够直接支持前端图形化展示和后续专家编辑。

### 4.5 Step 4：结构校验与重试

核心函数：`validate_full()`、`validate_structure()`

实现位置：

- [validator.py]


实现说明：

- 在草稿生成后，先进行结构层面的自动校验；
- 如果存在错误，则把错误信息反馈给生成阶段重新生成；
- 目前最大重试次数为 2 次，即最多尝试 3 轮。

当前结构校验覆盖的主要规则包括：

- 顶事件唯一性；
- 基本事件不能再有子节点；
- 中间事件必须存在子节点；
- 非基本事件逻辑门必须是 `OR` 或 `AND`；
- 节点名称不能为空；
- 因果链不能形成循环；
- `event` 对象字段完整性检查；
- 文档溯源缺失提示。

这一机制显著提升了生成结果的工程可用性，而不是仅停留在“模型能生成一个 JSON”。

### 4.6 Step 5：基于历史修正的定向修复

核心函数：

- `get_relevant_corrections()`，见 [diff_analyzer.py]
- `format_corrections_for_repair()`，见 [diff_analyzer.py]
- `repair_fault_tree()`，见 [generator.py]

实现说明：

- 在草稿树生成完成后，系统会提取草稿中的全部节点名称；
- 到 `corrections` 集合中按 `node_name` 批量检索相关历史专家修正记录；
- 对命中的修正进行加权排序，再转成面向 LLM#3 的“定向修复提示”；
- LLM#3 只允许对提示中涉及的节点做最小化修改，禁止大范围重构整棵树；
- 修复完成后再次做结构校验，若失败则回退到草稿版本。

这一步是当前系统区别于普通“生成式输出”的核心能力，体现为“带经验迁移的生成优化”。

### 4.7 Step 6：最终逻辑校验

核心函数：`validate_semantics()`

实现位置： [validator.py]

实现说明：

- 在结构无误的前提下，再由大模型进行语义层检查；
- 主要关注逻辑门使用是否合理、因果关系是否成立、底事件是否足够具体、是否存在明显遗漏。

最终返回统一的校验结果对象：

- `passed`
- `error_count`
- `warning_count`
- `info_count`
- `issues`

这使得前端和评测方可以清晰看到该树是否达到可交付状态。

## 5. 数据结构与存储设计

### 5.1 故障树主表

实现位置： [database.py]

集合：`fault_trees`

主要字段：

- `_id`
- `top_event`
- `created_at`
- `updated_at`
- `current_version`
- `status`

作用：

- 记录一棵故障树的主索引信息；
- 管理当前版本状态。

### 5.2 版本表

实现位置： [database.py]

集合：`fault_tree_versions`

主要字段：

- `tree_id`
- `version`
- `is_ai_generated`
- `created_at`
- `editor`
- `description`
- `tree_data`

作用：

- 保存 AI 初始生成版本和专家修改版本；
- 支持版本历史、回滚和差异分析。

### 5.3 知识片段表

实现位置： [database.py]

集合：`chunks`

作用：

- 存储切分后的工业知识片段；
- 支撑按关键词召回相关证据；
- 为节点中的 `documents` 提供溯源来源。

### 5.4 修正经验表

实现位置： [diff_analyzer.py]

集合：`corrections`

主要字段：

- `tree_id`
- `ai_version`
- `expert_version`
- `top_event`
- `node_name`
- `node_type`
- `correction_type`
- `detail`
- `created_at`

作用：

- 将专家修正沉淀为可检索、可复用的“经验单元”；
- 为下一轮生成提供定向修复依据。

## 6. 概要设计

### 6.1 架构分层

本模块可划分为五层：

1. 接口层  
负责接收生成、保存、校验、回滚、查询等请求，对外提供 REST API。

2. 编排层  
以 `generate_fault_tree()` 为核心，负责串联知识检索、生成、修复、校验等流程。

3. 模型能力层  
由 3 个不同任务的大模型调用构成：
- LLM#1：知识抽取
- LLM#2：故障树生成
- LLM#3：基于历史修正的定向修复

4. 校验层  
负责结构校验和语义校验，保证结果符合 FTA 规范。

5. 数据层  
负责知识片段、故障树版本、专家修正经验的持久化存储。

### 6.2 处理流程设计

自动生成模块的输入、处理、输出如下：

- 输入：
  - 用户指定顶事件；
  - 用户补充生成要求；
  - MongoDB 中已导入的工业知识片段。
- 中间处理：
  - 关键词提取；
  - 文档召回；
  - 故障要素抽取；
  - 故障树结构生成；
  - 结构校验与重试；
  - 历史修正检索；
  - 定向修复；
  - 语义校验。
- 输出：
  - 标准化故障树 JSON；
  - 校验结果；
  - 溯源信息；
  - 版本记录。

### 6.3 输出格式设计

当前故障树输出采用统一 JSON 结构，见 [generator.py]。

其中：

- `nodeList` 表示节点集合；
- `linkList` 表示因果关系集合；
- 节点类型包含：
  - `top_event`
  - `intermediate_event`
  - `basic_event`

节点事件信息中保留以下工程化字段：

- 描述信息；
- 故障等级；
- 概率字段；
- 规则字段；
- 排查方法；
- 文档溯源。

这种设计兼顾了图形展示、逻辑分析、知识溯源和后续扩展。

## 7. 创新要点

结合当前代码实现和你提供的 `correction_learning_cn_v2.svg`，建议将创新点凝练为以下 3 点。

### 创新点 1：构建“知识抽取 + 结构生成 + 修正反馈”三阶段协同生成链路

传统方案往往直接让模型从文档生成故障树，容易出现结构漂移、证据不足和输出不稳定等问题。当前实现将生成拆解为三个有明确职责的阶段：

- LLM#1 负责从工业知识中提取故障事件与因果关系；
- LLM#2 负责依据 FTA 规范组装成标准化故障树；
- LLM#3 负责吸收历史专家修正经验，对草稿树进行定向修复。

这一设计使大模型从“直接生成结果”转变为“分工协同完成生成”，降低了复杂工业场景中一次生成失败的概率，也更适合答辩时说明系统的工程可靠性。

### 创新点 2：将专家编辑行为转化为可复用的修正知识，实现生成能力持续优化

这是本模块最核心的创新点。

当前系统不是把专家修改仅作为一次性人工操作，而是通过 [main.py] 和 [diff_analyzer.py]自动把“AI 初稿”和“专家终稿”的差异提炼为结构化修正经验，包括：

- 节点补充；
- 节点删除；
- 逻辑门修改；
- 名称术语修正；
- 因果连接补充；
- 错误连接删除。

这些经验被持久化到 `corrections` 集合中，下一次生成相似节点时可被自动检索并再次利用，从而让系统具备“越用越准”的持续优化能力。

你在材料里可以把这一点表述为：

“本系统提出一种基于专家修正反馈的故障树生成持续学习机制，将专家对 AI 结果的编辑行为转化为节点级修正规则，并在后续生成中进行定向迁移，实现生成质量的闭环提升。”

### 创新点 3：采用节点级加权检索与最小修改修复策略，兼顾学习效果与生成稳定性

与简单地把全部历史修正记录拼接进提示词不同，当前实现增加了两层控制：

1. 检索阶段按节点名称、节点类型、修正类型价值和时间新鲜度做加权排序，优先检索最相关、最有价值的修正经验；
2. 修复阶段明确约束 LLM#3 只能对命中的相关节点做最小化修改，禁止大范围重构整棵树。

这种“节点级检索 + 最小编辑修复”的设计，一方面提升了修正建议的针对性，另一方面避免历史经验污染当前场景，增强了自动生成模块的稳定性和可控性。

## 8. 可直接用于材料提交的概要设计表述

可将本模块概要设计概括为：

“本模块面向工业设备故障树自动生成场景，采用‘知识检索驱动 + 大模型分阶段生成 + 专家修正闭环学习’的总体方案。系统首先从工业文档切片库中召回与顶事件相关的知识片段，利用大模型提取故障事件、因果关系与逻辑门信息；随后依据 FTA 规范生成包含顶事件、中间事件、底事件及因果连接的标准化故障树 JSON；在生成后，系统通过结构校验和语义校验保障结果质量。针对专家修改结果，系统进一步自动分析 AI 初稿与专家终稿之间的差异，将其沉淀为节点级修正经验库，并在后续生成中通过节点级加权检索和定向修复机制优化生成结果，从而形成‘生成 - 编辑 - 学习 - 再生成’的持续优化闭环。” 

## 9. 当前实现的优势与边界

### 9.1 当前优势

- 已具备可运行的后端接口和版本化存储；
- 已形成相对完整的生成闭环；
- 已支持知识溯源；
- 已支持专家修正学习；
- 已具备结构校验与语义校验双层机制；
- 输出格式明确，便于前端直接可视化。

### 9.2 当前边界

- 当前知识召回主要基于关键词匹配，后续可升级为向量检索或混合检索；
- 修正学习目前以节点名称为主键，后续可进一步结合设备类型、顶事件上下文和语义相似度；
- 当前“学习”主要表现为提示增强与定向修复，尚未发展为真正的参数微调；
- 结构准确率、关联准确率仍需结合具体数据集做离线评测和量化统计。



# 知识图谱故障触发环处理方案

## 结论

KB 模块完成文件内聚类、跨文件聚类和图谱导入后，**不应全局删除普通故障触发环**。

原因是：普通环中的不同边可能来自不同文件或不同文件版本。全局删除某一条边，可能破坏另一个文件视图下本来合理的因果证据。

KB 阶段只允许清理确定错误的自环：

```text
A -> A
```

普通有向环，例如：

```text
A -> B
B -> A
```

或：

```text
A -> B -> C -> A
```

应交给 GNR 阶段，在“用户当前选择文件视图”中临时处理。

## KB 阶段

KB 阶段只做自环删除：

```powershell
.\.venv\Scripts\python.exe cleanup_neo4j_self_loops.py `
  --node-label EntityCluster `
  --relation-level clustered `
  --dry-run
```

确认后执行：

```powershell
.\.venv\Scripts\python.exe cleanup_neo4j_self_loops.py `
  --node-label EntityCluster `
  --relation-level clustered
```

普通故障触发环只生成诊断报告，不修改 Neo4j：

```powershell
.\.venv\Scripts\python.exe cleanup_neo4j_causal_cycles.py `
  --report-file output\causal_cycle_diagnostic.json
```

`cleanup_neo4j_causal_cycles.py --apply` 已被禁用，避免误删全局图谱关系。

## GNR 阶段

GNR 根据用户选择的文件版本构造当前视图：

```text
selected_file_version_ids = [...]
```

然后只在该视图下检查：

```text
(:EntityCluster:FaultEvent)-[:CLUSTERED_CAUSES]->(:EntityCluster:FaultEvent)
```

如果检测到环，GNR 不写回 Neo4j，而是在本次生成流程的内存 subgraph 中临时停用一条最弱边。

新开聊天、重新选择文件、或文件范围变化时，GNR 会重新构造视图并重新判断，不继承上一次的临时停用结果。

## 临时取舍策略

对每个环，GNR 选择最弱边临时停用。强度依据包括：

1. `certainty=certain` 强于 `possible`；
2. `polarity=positive` 强于 negative；
3. evidence 数量越多越强；
4. source_relation_ids 越多越强；
5. source_file_version_ids 越多越强；
6. 同一句 evidence 中出现“源事件 ... 触发/导致/引起 ... 目标事件”时，大幅加分；
7. 跨 chunk 拼接且没有同句方向证据时降权。

例如：

```text
储能仓环境温度过高，触发系统关机。
```

强支持：

```text
储能仓环境温度过高 -> 系统关机
```

不支持：

```text
系统关机 -> 储能仓环境温度过高
```

## GNR 日志

如果当前选择文件视图中存在环，GNR 会打印：

```text
[graph-subgraph] temporarily disabled N causal cycle edges in selected file view
```

这些边只在本次生成中被忽略，Neo4j 原始关系仍然保留。


# 标注审核工具原型计划

## 目标

本目录用于承载“AI 初标 → raw 记录保存 → CSV 人工审核 → 最终 JSONL 转换”的标注数据流程。当前阶段先实现可用原型，用于小规模生成和审核训练样本，后续再根据微调实验结果调整 schema。

## 总体流程

```text
MongoDB chunks
→ generate_ai_annotation_dataset.py 调用 LLM 初标
→ 保存 raw_ai_annotations.jsonl
→ 生成 ai_annotation_draft.csv
→ Streamlit 人工复核
→ 保存 reviewed_annotations.csv
→ convert_reviewed_csv_to_jsonl.py 转换
→ final_annotations.jsonl
```

## 样本组织方式

每个标注样本对应一个 target chunk，并可带一个前文 context chunk：

```text
context_text = 前一个满足滑动窗口条件的 chunk，可为空
text = 当前 target chunk
```

即使存在 `context_text`，标注主体仍然是当前 `text`。单独只出现在 `context_text` 中、且没有和当前 `text` 发生关系的实体和关系不标注。

滑动窗口的具体判定规则记录在：

```text
滑动窗口判定规则.md
```

## 大模型输出格式

大模型不直接输出最终 JSON，而是输出便于解析和人工检查的四段式文本：

```text
[CONTEXT_ENTITY]
mention | type | normalized_name | start | end | annotation_role | evidence

[TARGET_ENTITY]
mention | type | normalized_name | start | end | evidence

[RELATION]
source | relation_type | target | cross_chunk | involved_chunk_ids | evidence | polarity | certainty

[LOGIC_GROUP]
logic_type | members | result | involved_chunk_ids | evidence
```

这样做的原因：

1. 比完整 JSON 更省 token；
2. 人工检查更直观；
3. 格式损坏时更容易容错解析；
4. 程序可以再统一转换为最终 JSONL。

## 实体与关系类型

实体类型固定为 5 类：

```text
故障事件
故障类别
报警码
维修方法
触发规则
```

关系类型固定为 7 类：

```text
故障触发
故障表征
故障分类
故障处理
规则触发
参与组合
组合导致
```

其中普通关系在 `[RELATION]` 中输出：

```text
故障触发：故障事件 -> 故障事件
故障表征：报警码 -> 故障事件
故障分类：故障事件 -> 故障类别
故障处理：维修方法 -> 故障事件
规则触发：触发规则 -> 故障事件
```

组合关系在 `[LOGIC_GROUP]` 中输出。一个逻辑组等价于：

```text
members --参与组合--> 逻辑组
逻辑组 --组合导致--> result
```

## CSV 中间格式

`ai_annotation_draft.csv` 和 `reviewed_annotations.csv` 采用“一行一个样本”的格式。主要字段包括：

| 字段 | 说明 |
| --- | --- |
| sample_id | 样本 ID |
| file_id | 来源文件 ID |
| chapter_id | 章节名称或章节 ID |
| source_type | 来源类型 |
| context_chunk_id | 上下文 chunk ID，可为空 |
| target_chunk_id | 当前 chunk ID |
| context_text | 上下文原文，可为空 |
| text | 当前 chunk 原文 |
| context_entities_json | 上下文实体数组 JSON 字符串 |
| target_entities_json | 当前实体数组 JSON 字符串 |
| relations_json | 关系数组 JSON 字符串 |
| logic_groups_json | 逻辑组数组 JSON 字符串 |
| status | 审核状态 |
| reviewer_notes | 人工备注 |

CSV 只是人工审核中间格式。系统最终使用 `convert_reviewed_csv_to_jsonl.py` 转换为 JSONL。

## Streamlit 审核工具

`app.py` 提供人工复核界面：

1. 左侧导入 CSV、筛选状态、选择样本；
2. 中间展示 `context_text` 和 `text`，文本可复制；
3. 右侧编辑上下文实体、当前实体、关系和逻辑组；
4. 支持保存当前样本、标记已审核、上下切换样本；
5. 切换样本时自动保存当前修改。

审核状态包括：

```text
draft
reviewing
reviewed
needs_fix
```

## 最终 JSONL

最终 JSONL 结构参考 `sample-example.jsonl`，核心字段为：

```text
sample_id
file_id
chapter_id
context_chunk_id
target_chunk_id
context_text
text
context_entities
target_entities
relations
logic_groups
```

转换脚本会做基础校验：

1. `sample_id`、`target_chunk_id` 非空；
2. 关系的 source / target 必须能在实体列表中找到；
3. 逻辑组的 members / result 必须能在实体列表中找到。

## 近期计划

1. 用少量真实 chunks 跑通完整流程；
2. 检查 AI 初标输出是否稳定符合四段式格式；
3. 通过 Streamlit 审核 50-100 条样本，验证 schema 是否足够；
4. 累积 800-1000 条高质量 reviewed 样本；
5. 使用 Qwen LoRA / efficient_sft 做第一版微调实验；
6. 根据测试集表现调整实体类型、关系类型、prompt 和数据配比。
